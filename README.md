# Soroban-Audit-CLI
A command-line tool that runs static analysis and security checks on Soroban smart contracts before deployment — flagging common vulnerabilities, gas inefficiencies, and best-practice violations.
